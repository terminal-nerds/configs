{
	"$schema": "https://json.schemastore.org/package",
	"private": true,
	"type": "module",
	"name": "@terminal-nerds/configs",
	"license": "MIT",
	"homepage": "https://github.com/terminal-nerds/configs",
	"repository": "terminal-nerds/configs.git",
	"bugs": "https://github.com/terminal-nerds/configs/issues",
	"engines": {
		"node": ">=16",
		"pnpm": ">=6"
	},
	"turbo": {
		"pipeline": {
			"build": {
				"dependsOn": [
					"^build"
				],
				"outputs": [
					"dist/**"
				]
			},
			"dev": {
				"cache": false
			}
		}
	},
	"scripts": {
		"build": "turbo run build",
		"format:pkgs": "syncpack format"
	},
	"devDependencies": {
		"syncpack": "^5.8.15",
		"turbo": "^1.0.23"
	}
}
